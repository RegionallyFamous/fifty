# Copy this file to .env and fill in your values.
# .env is gitignored — never commit real keys.
#
# Required for product photo generation (GPT Image 2):
OPENAI_API_KEY=sk-...

# Required for LLM-assisted microcopy + front-page generation:
ANTHROPIC_API_KEY=sk-ant-...

# Optional — alternative image generation backend (deprioritised when OPENAI_API_KEY is set):
# FAL_KEY=...

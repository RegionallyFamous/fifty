{
  "_comment": "Example manifest for bin/design-batch.py. Each entry MUST set exactly one of 'spec' or legacy 'prompt' (prompt requires FIFTY_ALLOW_NON_MILES_SPEC=1). Optional 'slug_hint' names the worktree before design.py runs. Top-level 'concurrency' (1-4) and 'model' are optional; CLI flags override.",
  "concurrency": 1,
  "themes": [
    {
      "spec": "specs/batch-example/midcentury.json",
      "slug_hint": "midcentury"
    },
    {
      "spec": "specs/batch-example/japandi.json",
      "slug_hint": "japandi"
    },
    {
      "spec": "specs/batch-example/risozine.json",
      "slug_hint": "risozine"
    }
  ]
}
